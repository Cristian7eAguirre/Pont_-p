3-A-

O ponteiro p não foi iniciado com o endereço de nenhuma variável válida antes de (*p=valor).

3-B/C-

#include <stdio.h>

int main () {
    
    int valor= 50;
    int *p= &valor;
    
    *p= 100;
    printf("%d\n", *p);
    
    return 0;
}
